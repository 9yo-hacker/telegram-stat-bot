# 🤖 Chat Economy Bot

Telegram-бот для чатов с **репутацией, дуэлями, удачей и статистикой активности**.  
Со временем в чате появляется **своя мини-экономика**: репутация, баффы, риск и награды.

Бот рассчитан на **живые чаты**, минимальный спам и понятный UX.

---

## 🚀 Возможности

### 🧠 Статистика чата
- `/whereall` — активность участников за **24 часа**
- `/whereall week` — активность за **7 дней**
- `/whereall month` — активность за **30 дней**
- `/interesting` — **слово недели** (частотное слово чата)

> Логируются **только обычные сообщения**,  
> команды (`/rep`, `/duel` и т.п.) **не влияют на статистику**.

---

### ⭐ Репутация
- `+` **ответом на сообщение** — повысить репутацию
- `-` **ответом на сообщение** — понизить (если разрешено)
- `/repme` — посмотреть свою репутацию
- `/toprep` — топ по репутации

Особенности:
- cooldown на голосование
- нельзя голосовать за себя
- работает **только через reply**, без мусорных команд

---

### 🎰 Удача (Luck)
- `/luck` — слот-машина с репутацией
- Возможные награды:
  - репутация
  - баффы на будущие дуэли
- Кулдаун на использование

Баффы:
- +точность
- +HP
- +патроны
- +шанс крита

> Бафф применяется **один раз**, автоматически, в следующей дуэли.

---

### 🤠 Дуэли
- `/duel @user` — вызвать участника
- Принятие / отказ — **через кнопки**
- Пошаговые раунды:
  - 🔫 выстрел
  - 🎯 прицел
  - 🕺 уклон
  - 🔄 перезарядка
  - 🩹 лечение
  - 🏳️ сдача

Особенности:
- таймер на раунд
- критические попадания
- уклоны и осечки
- эпичные текстовые события
- награда репутацией победителю

---

### 🛠 Управление чатом
- `/on` — включить бота
- `/off` — выключить бота
- `/tz Europe/Moscow` — часовой пояс чата
- `/quiet 30m | 2h | 1d | off` — тихий режим

---

## 🧩 Техническая часть

**Стек:**
- Python 3.10+
- aiogram 3.x
- SQLite (WAL mode)
- async / await

**Хранение данных:**
- репутация
- сообщения (для статистики)
- слова и фразы
- дуэли
- баффы удачи

Данные сохраняются локально в `bot.db`.

---

## ⚙️ Установка и запуск

### 1. Установить зависимости
```bash
pip install aiogram
```
### 2. Переменные окружения
``` bash
export BOT_TOKEN=your_telegram_bot_token
export BOT_TZ=Europe/Moscow
```
### 3. Запуск
```bash
python bot.py
```
## ☁️ Деплой

Бот корректно работает на:
- Railway
- VPS
- Docker (опционально)
- SQLite используется в режиме WAL — безопасно для продакшена при одном инстансе.
